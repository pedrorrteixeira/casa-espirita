# Dependencias
node_modules/

# Credenciais OAuth do clasp — NUNCA versionar.
# O .clasp.json (que tem so o scriptId) pode e deve ser versionado.
.clasprc.json
*.local.json

# Ruido
npm-debug.log*
.DS_Store
Thumbs.db

# Material de origem da logo. O que o sistema usa é src/ui/logo.html,
# com a imagem embutida — ver o comentário lá.
logo.png
logo-96.png
